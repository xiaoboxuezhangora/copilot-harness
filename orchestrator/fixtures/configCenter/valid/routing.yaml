version: 4

defaults:
  default_route:
    provider_id: copilot
    model: gpt-5-mini
    runtime: copilot-sdk
    reason: "baseline default model"
    fallback_chain: []

provider_constraints: []
jira_model_routes: []
profile_routes: []
task_routes: []

route_precedence:
  - provider_constraints
  - data_classification
  - jira_model_routes
  - profile_routes
  - task_routes
  - defaults.default_route

total_budget_ms: 30000
